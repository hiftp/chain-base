This module creates a user for the user (e.g. an accountant), similar to what Curq does.
This user gets the groups defined in groups.txt,
and an e-mail to set a password for the user on creation of the container.